# Foundry build artifacts — never commit (same rule as programs/target).
cache/
out/
broadcast/

# npm pack output (.tgz tarballs) — build artifacts, never commit.
# These are the files uploaded to npm; they are reproducible with `npm pack`.
*.tgz

# Node/npm local noise inside the packages.
node_modules/
npm-debug.log*
.npm/

