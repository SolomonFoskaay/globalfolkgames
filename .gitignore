offlineUseOnly/
node_modules

# Build output (generated by `npm run build`; Vercel builds it fresh)
dist
# EXCEPTION: the GGI SDK browser bundle is a SHIPPED package asset (it is what a
# plain HTML page loads from the npm CDN), so it must be committed. Everything
# else under a dist/ folder stays ignored.
!foskaay-ggi/packages/sdk/dist/
!foskaay-ggi/packages/sdk/dist/ggi-sdk.browser.js

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env files (never commit secrets)
.env
.env.*
!.env.example
.env.local
.env.*.local

# Sponsor spend ledger (relay state, machine-local)
.gfg-spend-ledger.json
scripts/gfg-referrals.json
scripts/gfg-handles.json
scripts/gfg-signups.json
scripts/gfg-comp-wins.json
scripts/gfg-comp-entries.json
scripts/.gfg-agm-registry.json

# Server-side probe log (dashboard leak/ops observations; machine-local)
.gfg-probe-log.jsonl

# Editor / OS
.vscode/
.idea/
.DS_Store

# Solana / Anchor keys & artifacts (NEVER commit a wallet keypair)
.anchor/
**/target/
*.keypair.json
*-keypair.json
id.json
~/.config/solana/
scripts/.gfg-agm-ids.json
.gfg-mp-lobby.json
.gfg-arc-spend.json
.gfg-arc-batch.json
