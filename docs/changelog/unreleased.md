- M5 v3 PremiumBooster: a 72h unlimited-lives booster bought with premium spendable points. One-plan flow (no win multiplier). Gasless on the ER.
- PremiumPoints account layout bumped to v3 (adds booster_active_until). Permissionless + idempotent upgrade path: v2 -> upgrade_premium_points_v3, v1 -> upgrade_premium_points (same program id, additive, solana-upgrade-safety compliant).
- Level 2 pricing (owner 2026-08-22): launch discount $3 -> $5; Nigeria N4,000 -> N5,000. Activation stays 5,000P premium spendable.
- NEW Level 3 (3x) plan (owner 2026-08-22): actual $20, discount $10 / N13,500, Nigeria N8,000. 15 lives/day, 300P/day daily reward, 3x win multiplier (100P base -> 300P), 1.5x earn-competition final-points boost. Activation premium cost TBD (proposed 10,000P). Plans remain config-driven (ladder = data, not code).
- Earn competition framework (M7, owner 2026-08-22): everything configurable on the admin creator UI (no code); base subscription requirement is a MULTI-SELECT allowed-level pick (L2+L3+L5, any-of / all-of combinators); DURATION dropdown (6h..1m, not fixed 24h; launch instance 72h); FINAL-POINTS RANKING (R17): winner position = Total Points x LIVE plan boost (L3 1.5 / L2 1.0), re-read at every render so a mid-window downgrade re-boosts instantly and an L1 drop hides the player from the board (not removed) with no position at the final freeze.- Affiliate updated (owner 2026-08-22): share = 20% of the referred plan's PAYABLE USD price, read LIVE from the config-driven plan ladder at settle (L2 $5 -> $1.00, L3 $10 -> $2.00; a plan price change auto-updates the share, never fixed to a plan). Replaces the old fixed 15% / $3 anchor. Historical rows stay as recorded.
