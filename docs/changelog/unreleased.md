- On-chain points (Scope B): add a record_points-style instruction to the same delegated gfg-dice program so points/rewards are recorded on-chain, keyed to the proof roll (match_id signature). ER is the gasless execution layer for this (same delegated program), VRF stays the randomness primitive only.
- On a win, the client submits the points record via the ER queue (session key signs, gasless); the on-chain tx signature becomes the authoritative receipt and is stored in Supabase as match_id for aggregation + de-dupe.
- Supabase stays the aggregation / fallback source of truth across devnet wipes (re-sync on redeploy); the on-chain record is the permanent verifiable layer.
- Scope C (optional bundle): on-chain finish-order record so a full game result (1st..4th) is also committed, not just the reward points.
